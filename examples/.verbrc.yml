# ===============================================
# Verb config > https://github.com/assemble/verb
# ===============================================

# Add a .travis.yml file if one doesn't exist?
travis:   true

# This metadata is used in HTML examples
title:    Verb config file
layouts:  test/fixtures/html
includes: test/fixtures/html/includes
