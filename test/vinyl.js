var fs = require('fs');
var path = require('path');

/**
 * Placeholder for when assemble-core is updated with the latest vinyl-fs
 */

// var cwd = path.resolve(__dirname, 'vinyl');
// fs.readdirSync(cwd).forEach(function(file) {
//   if (/\.js$/.test(file)) {
//     require(path.resolve(cwd, file));
//   }
// });
